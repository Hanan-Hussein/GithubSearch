import { RepositorySkeleton } from './repository-skeleton';

describe('RepositorySkeleton', () => {
  it('should create an instance', () => {
    expect(new RepositorySkeleton()).toBeTruthy();
  });
});
